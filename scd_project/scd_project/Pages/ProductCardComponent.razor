@page "/card"
@using scd_project.Models

<style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #22162B;
        margin: 0;
        padding: 0;
    }

    .container {
        max-width: 960px;
        margin: 2rem auto;
        padding: 0 1rem;
        color: white;
        position: relative;
    }

    .top-bar {
        display: flex;
        justify-content: center;
        align-items: center;
        position: relative;
        margin-bottom: 2rem;
    }

    

    .card {
        width: 300px;
        height: 420px;
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        display: flex;
        flex-direction: column;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        background-color: #451F55;
        color: white;
    }

        .card:hover {
            transform: translateY(-7px);
            box-shadow: 0 10px 25px rgba(229, 79, 109, 0.5);
        }

    .card-img-top {
        width: 100%;
        height: 180px;
        object-fit: cover;
    }

    .card-body {
        flex: 1;
        display: flex;
        flex-direction: column;
        padding: 1rem;
    }

    .card-title {
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
        color: #F8C630;
    }

    .card-text {
        font-size: 1rem;
        color: #E54F6D;
        margin-bottom: 1rem;
        flex-grow: 0;
    }

    .mt-auto {
        margin-top: auto;
    }

    .btn-primary {
        background-color: #724E91;
        border: none;
        color: #fff;
        border-radius: 2rem;
        padding: 0.75rem 2rem;
        font-weight: 600;
        font-size: 1.1rem;
        width: 100%;
        transition: background-color 0.3s ease;
    }

        .btn-primary:hover {
            background-color: #E54F6D;
            color: #22162B;
        }
</style>

<div class="container">
    

    <div class="card">
        <img src="@product.Images.First()" class="card-img-top" alt="@product.Name" />
        <div class="card-body">
            <h5 class="card-title">@product.Name</h5>
            <p class="card-text">@product.Description</p>
            <div class="mt-auto">
                <button class="btn btn-primary">View Details</button>
            </div>
        </div>
    </div>
</div>

@code {
    private string searchTerm = "";

    [Parameter]
    public Product product { get; set; }

    private void OnFilterClicked()
    {
       
        Console.WriteLine("Filter clicked!");
    }
}
